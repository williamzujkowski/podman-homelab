---
# Secrets and sensitive configuration
# WARNING: This file should be encrypted with ansible-vault in production

# Cloudflare API credentials for Let's Encrypt DNS challenge
cloudflare_email: ""
cloudflare_api_token: ""

# Grafana secret key (generate with: openssl rand -base64 32)
grafana_secret_key: "SW2YcwTIb9zpOOhoPsMm"

# Default passwords (CHANGE THESE IN PRODUCTION)
default_passwords:
  grafana_admin: admin
  prometheus_admin: admin

# SSH keys for automation
automation_ssh_keys: []

# Tailscale configuration
tailscale_auth_key: ""
tailscale_tags:
  - "tag:homelab"
  - "tag:production"