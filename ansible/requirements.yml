---
# Ansible Galaxy requirements for Pi homelab

collections:
  - name: community.general
    version: ">=7.0.0"
  - name: containers.podman
    version: ">=1.10.0"
  - name: community.crypto
    version: ">=2.15.0"
  - name: ansible.posix
    version: ">=1.5.0"

roles: []